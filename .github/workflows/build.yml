#This is a YAML file

on: push
jobs:
    first-job:
        runs-on : ubuntu-latest
        steps :
            - run: node --version
            - run: npm --version
            - uses: actions/checkout@v4
              with:
                path: server
            - run: node app.js
              working-directory: server